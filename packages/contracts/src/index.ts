/**
 * @ecomsaas/contracts
 * API contracts, DTOs, and shared protocol types for the EcomSaaS platform
 *
 * This package belongs to the interface-adapter layer (outer ring).
 * It depends on @ecomsaas/domain (inner ring) — never the reverse.
 */

export * from './dtos';
export * from './common';
