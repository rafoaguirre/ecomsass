/**
 * @ecomsaas/infrastructure
 *
 * Shared infrastructure utilities for the EcomSaaS platform.
 * Provides framework-agnostic abstractions for common infrastructure concerns.
 *
 * Usage:
 * ```typescript
 * // Import specific modules via subpath exports
 * import { createLogger } from '@ecomsaas/infrastructure/logger';
 * import { createHttpClient } from '@ecomsaas/infrastructure/http';
 * import { createCache } from '@ecomsaas/infrastructure/cache';
 * ```
 */

// Note: This package primarily uses subpath exports.
// Import from specific modules rather than the main entry point.

export const version = '0.1.0';
