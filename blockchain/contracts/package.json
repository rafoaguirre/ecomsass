{
  "name": "@ecomsaas/smart-contracts",
  "version": "0.1.0",
  "description": "EcomSaaS Smart Contracts — Foundry / Solidity",
  "private": true,
  "scripts": {
    "prebuild": "forge install foundry-rs/forge-std --no-git",
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "fmt": "forge fmt"
  }
}
